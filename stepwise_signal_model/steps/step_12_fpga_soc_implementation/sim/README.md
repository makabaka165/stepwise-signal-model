# Simulation Scripts

`run_iverilog_sim.sh` runs all three module-level testbenches with iverilog when available.

`run_xsim_sim.tcl` is a Vivado xsim-oriented script skeleton. It compiles the same RTL and testbench files and launches each testbench simulation.

Generated waveform and simulator files are ignored by `.gitignore`.
