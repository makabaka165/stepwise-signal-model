# RTL Placeholder

This directory is reserved for Step14 AXI4-Stream wrapper RTL in later stages.

Current Step14.0 intentionally does not implement AXI RTL. It only documents
the interfaces, layering, and reuse boundaries for the future
`dbf_axis_datapath`, `dbf_w_provider_rom`, and `dbf_axis_system_top` modules.
