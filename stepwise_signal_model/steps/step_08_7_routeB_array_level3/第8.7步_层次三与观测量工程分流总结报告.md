# 第8.7步：层次三与观测量工程分流总结报告

## 0. 文档定位

本文用于把第 8.7 步第 1 部分到第 7B 部分的理论目标、路线演化、公式、代码入口、实验结论、工程分流逻辑和 runtime 验证统一整理到一份文档中。后续论文写作、答辩整理或工程回看时，不再需要逐份翻阅阶段记录，只需先读本文，再按需回查单独记录。

本文整合的核心资料包括：

- 第 8.7 各阶段记录：
  - `第8.7步_1_层次三最小验证记录.md`
  - `第8.7步_2_二维rank1协方差拟合最小验证记录.md`
  - `第8.7步_3_common-el二维covfit稳定性验证记录.md`
  - `第8.7步_4_common-el重聚焦与层次二fallback验证记录.md`
  - `第8.7步_5_一般源模型分流边界验证记录.md`
  - `第8.7步_6_观测量驱动工程分流验证记录.md`
  - `第8.7步_6B_观测量分流去真值化与置信度校准记录.md`
  - `第8.7步_7_预算感知级联早停工程分流验证记录.md`
  - `第8.7步_7B_真实lazy级联运行时间验证记录.md`
  - `第8.7步前四部份路线总结.md`
- 第 8.7 各结果目录下的 `keypoints.csv`
- 第 8.6 收束主文档：
  - `../step_08_6_routeB_array_level2_3/第8.6步_最终整理版_理论代码结果一体化报告.md`
  - `../step_08_6_routeB_array_level2_3/第8.6步_理论公式与代码对应关系.md`
  - `../step_08_6_routeB_array_level2_3/第8.6步_工程化算法分流方案.md`

说明：提示词中提到的 `第8.7步_一般源模型与层次三验证目标.md` 在当前仓库中未以该名称单独保留。其作用已由第 8.7 第 1 部分记录、`第8.7步前四部份路线总结.md` 以及 `相干模型的一般化推进.md` 承接。

---

## 1. 摘要

第 8.7 步的最终定位不是“再发明一个单一的 2D-MUSIC 算法”，而是在第 8.6 层次二已经收束的前提下，利用 65×32 二维圆柱阵信息补救层次二的一维俯仰压缩边界，并把这部分二维信息最终收束成一套保守的、观测量驱动的工程分流雏形。第 8.6 的主线已经明确：65 列动态圆柱工作子阵、32 层按 `el_assumed` 相干合成、真实圆柱流形 FBSS-MUSIC 主链路，以及在完全相干小方位间隔下启用的 `rank1 equal-phase covfit fallback`。第 8.7 证明，这条层次二链路的边界主要来自两类场景：一类是两个目标存在较大俯仰差，此时提前压缩 32 层会直接丢失关键二维几何信息；另一类是两个目标同俯仰但上游 `el_assumed` 不可靠，此时层次二的 32 层相干合成方向错误，连 rank1 fallback 也会失效。

围绕这两个边界，第 8.7 形成了两种不同但互补的层次三作用方式。对于大俯仰差目标，保留二维阵列后的 `2D-MUSIC / pair-el local covfit` 可以直接分辨并恢复结果；对于同俯仰但 `el_assumed` 错误的场景，二维信息更适合先用于估计公共俯仰 `el_hat`，再回到第 8.6 已成熟的层次二 `rank1 fallback`，而不是直接硬做不稳定的 3D common-el covfit。随后，第 8.7 又在一般源模型
$$
s_2(t)=\beta e^{j\phi}\left(\rho s_1(t)+\sqrt{1-\rho^2}v(t)\right)
$$
下系统验证了边界：弱目标、近反相、大俯仰差、非完全相干场景对应不同处理路线，最终把“标签化分流”推进为“只依赖观测量的工程分流”。第 6B 部分进一步完成去真值化 route gating 与置信度校准，得到 `observable dispatch success = 0.732`、`oracle dispatch success = 0.798`、`gap = -0.066`、`false-high-confidence = 0`、`boundary-missed = 0`，说明当前第 8.7 已经可以收束为“层次三信息 + 层次二 fallback + 观测量 dispatch”的保守工程链路。第 7 部分把全 route offline dispatch 改写为预算感知级联早停策略，证明在 estimated cost 层面可以保持 `success = 0.731` 且降低约 `0.355` 的计算代价；第 7B 部分进一步实现真实 lazy execution 和 wall-clock 计时，保持 `lazy success = 0.731`、`false-high-confidence = 0`、`boundary-missed = 0`，并将平均 runtime 从 `0.629 s` 降到 `0.405 s`。因此第 7B 是当前第 8.7 工程 runtime 的最终主线版本，不建议此时继续马上堆 V2、complex-gain 或完整 4D 搜索。

---

## 2. 第 8.6 到第 8.7 的衔接

### 2.1 第 8.6 的层次二主线

第 8.6 最终收束为以下工程链路：

```text
65 列动态圆柱工作子阵
+ 32 层按 el_assumed 相干合成
+ 真实圆柱流形 FBSS-MUSIC
+ rank1 equal-phase covfit fallback
```

其理论和代码前提是：

1. Route-B 的有效方位工作孔径不是旧探索中的“中心 32 列”，而是 `Q=65` 列动态圆柱工作子阵。
2. 层次二的输入不是严格 ULA，而是 65 列真实圆柱阵几何。
3. 32 层不是丢弃不用，而是按照俯仰先验 `el_assumed` 先做相干合成，再把二维阵列压缩成 `Q x T` 的一维方位观测。
4. 默认主链路仍是基于真实圆柱流形的 FBSS-MUSIC；rank1 equal-phase covfit 只在 MUSIC 双峰合并时作为极端强相干 fallback。

### 2.2 第 8.6 已暴露的边界

第 8.6 的主文档和工程分流文档已经明确给出层次二的边界：

1. **大俯仰差边界**  
   当两个目标俯仰差增大时，一维层次二的 32 层预压缩不再合理；工程压力测试中 `el_b >= 2 deg` 时 rank1 fallback 已开始失效。

2. **俯仰先验错误边界**  
   当上游 `el_assumed` 明显错误时，32 层相干合成方向本身错了，后续一维 MUSIC 与 rank1 fallback 都会被拖垮。工程压力测试中 `el_assumed=10 deg` 已接近完全失效。

3. **幅度差 / 弱目标边界**  
   `beta=0.3` 开始明显退化，`beta=0.1` 近乎失败。弱目标不是简单换模型能解决的问题，而是动态范围和检测能力边界。

4. **近反相边界**  
   `phase=150/180 deg` 属于病态边界，rank1 equal-phase fallback 不适合继续强行解释。

5. **complex-gain / V2 不是当前主线**  
   第 8.6 已明确把 `complex-gain q`、V2 constrained covariance fitting 和层次三保留为未来扩展，不在层次二内继续堆自由度。

### 2.3 第 8.7 的任务

第 8.7 的任务可以概括为一句话：

> 使用 65×32 二维圆柱阵信息补救第 8.6 层次二边界，并最终形成工程分流策略。

这里的“补救”不是推翻第 8.6，而是补足两类层次二无法可靠覆盖的场景：

- 大俯仰差目标；
- 同俯仰但俯仰先验不可靠的目标。

---

## 3. 第 8.7 理论模型

### 3.1 一般双源模型

第 8.7 将一般相干双源模型统一写为：

$$
x(t)=a(\theta_1,e_1)s_1(t)+a(\theta_2,e_2)s_2(t)+n(t)
$$

其中第二个源满足：

$$
s_2(t)=\beta e^{j\phi}\left[\rho s_1(t)+\sqrt{1-\rho^2}v(t)\right]
$$

变量含义如下：

- $\beta$：幅度比；
- $\phi$：固定相位偏置；
- $\rho$：相干度；
- $v(t)$：与 $s_1(t)$ 独立的辅助源；
- $e_1,e_2$：两个目标俯仰角；
- $\theta_1,\theta_2$：两个目标方位角。

对应的源协方差可写为：

$$
R_s=
\begin{bmatrix}
P_1 & \rho\sqrt{P_1P_2}e^{-j\phi} \\
\rho\sqrt{P_1P_2}e^{j\phi} & P_2
\end{bmatrix}
$$

其中若 $P_2=\beta^2P_1$，则信号模型与上式完全一致。

第 8.7 的关键点不在于直接求解最一般的 $R_s$，而在于：

1. 把这个一般模型作为仿真和边界验证的统一框架；
2. 用它分析什么时候 MUSIC 仍可工作、什么时候需要 rank1/refocus、什么时候应直接给出边界或低置信度；
3. 暂不在当前阶段实现完整的 V2/general covariance fitting。

当前实验中的源序列实现采用确定性复指数基序列构造 `s1_base` 与 `v_base`，用于稳定复现实验边界趋势；它不是严格的 i.i.d. 复高斯源统计模型。当前噪声按 clean 观测总功率设定：

$$
\sigma^2=\frac{\mathrm{mean}(|y_{clean}(:)|^2)}{10^{SNR/10}}
$$

因此当前 SNR 是接收总 clean power SNR，而不是单目标固定源功率 SNR。该设定适合比较各 route 在同一观测功率水平下的相对表现；若要专门评估近反相导致的能量抵消，应另做固定噪声底或单源功率归一化实验。这里仅补充定义说明，不修改代码与历史结果。

### 3.2 二维圆柱阵 steering

第 8.7 保留真实二维圆柱阵 steering：

$$
a_{q,m}(\theta,e)=
\exp\left[
-jk\left(
X_{q,m}\cos e\cos\theta
+Y_{q,m}\cos e\sin\theta
+Z_{q,m}\sin e
\right)
\right]
$$

这里：

- $q$ 表示 65 列方位工作子阵索引；
- $m$ 表示 32 层俯仰索引；
- $(X_{q,m},Y_{q,m},Z_{q,m})$ 为真实圆柱阵几何坐标。

层次二与层次三的根本区别就在这里：

- **层次二**：先把 $m$ 维压缩掉，只保留 `Q x T` 的一维方位观测；
- **层次三**：保留 $(q,m)$ 二维阵列结构，做 az/el 联合建模。

### 3.3 二维子阵平滑

第 8.7 在二维上使用子阵平滑与 FBSS：

$$
S_{p,r}=S_{\phi,p}\otimes S_{z,r}
$$

$$
R_{2D,F}
=
\frac{1}{P_\phi P_z}\sum_{p,r}S_{p,r}R_xS_{p,r}^H
$$

$$
R_{2D,FB}
=
\frac{1}{2}\left(R_{2D,F}+J_{2D}\overline{R_{2D,F}}J_{2D}\right)
$$

其中

$$
J_{2D}=J_z\otimes J_\phi
$$

当前代码采用 MATLAB 列优先向量化 `sub(:)`，二维子阵矩阵维度为 $K_\phi \times K_z$。因此二维翻转矩阵在代码中写作：

```matlab
J_2D = kron(fliplr(eye(K_z)), fliplr(eye(K_phi)));
```

即公式应写为 $J_z \otimes J_\phi$。这一套二维平滑协方差是后续 `2D-MUSIC`、`pair-el local covfit` 和 common-el 相关实验的统一基础。

### 3.4 第 8.7 的总体思想

第 8.7 并没有直接走向“完整 4D 全局 coherent covfit”，原因是自由度过大且可辨识性不稳。相反，它采用了更工程化的路线：

1. 先用最小验证判断二维信息到底能解决哪类层次二边界；
2. 区分“大俯仰差”和“同俯仰但先验错误”两类问题；
3. 在一般源模型下验证弱目标、近反相、非完全相干等边界；
4. 最终把这些经验转成观测量驱动的工程分流。

---

## 4. 第 8.7 第 1 部分：minimal validation 总结

### 4.1 目标

第 1 部分的目标是：

```text
验证二维信息是否能补救层次二边界。
```

这一部分不追求完整工程算法，只做最小 proof-of-concept。

### 4.2 场景 A：大俯仰差

实验设计：

```text
el_a = 0
el_b = [0, 1, 2, 5, 10]
```

比较 route：

- `level2_rank1_fallback_baseline`
- `level2_el_bank_baseline`
- `level3_2d_fbss_music`
- `level3_oracle_el_rank1_covfit_upper_bound`

关键结果：

| 场景 | level2 rank1 | 2D MUSIC | oracle 2D covfit | 结论 |
|---|---:|---:|---:|---|
| `el_b >= 2 deg` | 0.000 | 1.000 | 1.000 | 大俯仰差应转入二维几何分离 |

解释：

- 层次二在 `el_b>=2 deg` 时开始失效；
- 2D-MUSIC 可以直接恢复分辨；
- oracle 2D rank1 covfit 也成功，说明二维信息本身是充分的。

### 4.3 场景 B：俯仰先验错误

实验设计：

```text
el_a = el_b = 0
el_assumed = [0, 2, 5, 10]
```

关键结果：

| 场景 | level2 rank1 | el-bank | 2D MUSIC | oracle 2D covfit | 结论 |
|---|---:|---:|---:|---:|---|
| `el_assumed >= 5 deg` | 0.000 | 0.192 | 0.000 | 1.000 | 二维信息有效，但 2D-MUSIC 仍单峰 |

解释：

- 俯仰先验错了以后，层次二的 32 层相干合成方向错误，rank1 fallback 直接被拖垮；
- 简单多 `el` 合成只带来轻微缓解；
- 同俯仰、强相干、小方位间隔时，2D-MUSIC 仍会单峰；
- 但 oracle 2D covfit 成功，说明二维信息中仍存在可恢复的公共俯仰信息。

### 4.4 第 1 部分结论

第 1 部分给出两个决定性的判断：

1. **大俯仰差** 是明确的二维几何问题，适合进入 2D-MUSIC / 2D covfit 路线。
2. **同俯仰但俯仰先验错误** 不是简单的“大俯仰差问题”，它更像“公共俯仰估计问题”；二维信息有效，但不能指望普通 2D-MUSIC 直接分开双峰。

---

## 5. 第 8.7 第 2 部分：二维 rank1 covfit 非 oracle 最小验证

### 5.1 目标

第 2 部分的目标是把第 1 部分中的 `oracle_el_rank1_covfit` 向非 oracle 推进一步。

### 5.2 两条新增路线

#### 路线 1：common-el rank1 covfit

直接搜索：

$$
(\theta_1,\theta_2,e_c)
$$

并假设：

$$
e_1=e_2=e_c
$$

这条路线针对“同俯仰但俯仰先验错误”的场景。

#### 路线 2：pair-el local rank1 covfit

先用 2D-MUSIC 得到两个二维峰，再做局部 refinement：

$$
(\theta_1,e_1),(\theta_2,e_2)
$$

这条路线针对“大俯仰差”的场景。

需要注意的是，当前代码中的 rank1 covfit 对子阵 steering 做了归一化：

$$
c_{p,r}=
\frac{a_{1,p,r}}{\|a_{1,p,r}\|}
+q\frac{a_{2,p,r}}{\|a_{2,p,r}\|}
$$

因此当前 covfit 是用于 DOA 搜索的归一化协方差匹配模型，不是完整的物理幅度估计模型。幅度差较大的弱目标问题仍然作为边界处理，未在当前阶段解决。

### 5.3 关键结果

| 场景 | level2 rank1 | 2D MUSIC | common-el covfit | pair-el local covfit | oracle | 结论 |
|---|---:|---:|---:|---:|---:|---|
| `el_b >= 2 deg` | 0.000 | 1.000 | 0.000 | 1.000 | 1.000 | 大俯仰差可由 2D-MUSIC + pair-local refinement 处理 |
| `el_assumed >= 5 deg` | 0.000 | 0.000 | 0.667 | - | 1.000 | common-el 直接 covfit 有潜力，但不稳 |

### 5.4 第 2 部分结论

1. **大俯仰差场景**  
   `pair-el local covfit` 成功，说明“2D-MUSIC 给粗初始化 + 局部二维 rank1 covfit refine”是可行路线。

2. **同俯仰且先验错误场景**  
   `common-el` 直接搜索虽然比纯 2D-MUSIC 更好，但离 oracle 仍有明显差距（约 `0.333`）。

因此，第 2 部分的核心结论是：

> 二维 covfit 模型是有效的，但 common-el 直接 3D 搜索的 objective landscape 仍不稳定。

---

## 6. 第 8.7 第 3 部分：common-el 3D covfit 稳定性分析

### 6.1 目标

第 3 部分专门回答一个问题：

```text
为什么 common-el 直接 3D covfit 不稳定？
```

### 6.2 尝试

为改善稳定性，第 3 部分做了两类尝试：

1. 保持原参数化：
   $$
   (\theta_1,\theta_2,e_c)
   $$
2. 改成中心角 + 间隔参数化：
   $$
   (\theta_c,\Delta\theta,e_c)
   $$

实验的核心目的不是追求更高成功率，而是分析 objective landscape。

### 6.3 关键结果

| 指标 | 数值 |
|---|---:|
| `B_l2_joint_ge5` | 0.000 |
| `B_elbank_joint_ge5` | 0.240 |
| `B_music2d_joint_ge5` | 0.000 |
| `B_common_old_joint_ge5` | 0.413 |
| `B_common_centersep_joint_ge5` | 0.387 |
| `B_oracle_joint_ge5` | 1.000 |
| `B_centersep_oracle_gap_ge5` | 0.613 |
| `true_objective_rank` | 186 |
| `objective_gap_true_vs_best` | 1.26e-6 |
| `num_competing_minima` | 99 |

### 6.4 判断

这一部分得出的判断很明确：

1. 把参数从 $(\theta_1,\theta_2,e_c)$ 改成 $(\theta_c,\Delta\theta,e_c)$ **没有本质改善**。
2. true candidate 在 objective map 中的排名很靠后，说明最优谷并不稳定围绕真实值形成。
3. `true-best gap` 很小，表示真实解与大量竞争谷几乎等价。
4. 存在大量近似最小值，说明问题不是“网格不够密”或“参数化写得不对”，而是可辨识性与 objective landscape 的根本边界。

### 6.5 第 3 部分结论

> common-el 直接 3D covfit 不是简单网格或参数化问题，而是 objective landscape / 可辨识性问题。它不适合作为当前主工程路线继续硬调。

这一步实际上帮助第 8.7 及时止损：不再把资源投入到直接 3D common-el covfit 的全局搜索上。

---

## 7. 第 8.7 第 4 部分：common-el refocus + layer2 fallback

### 7.1 核心思想

第 4 部分是第 8.7 最关键的转折点之一。

它放弃了“直接联合搜索 $(az_1,az_2,e_c)$”，转而采用：

```text
先估公共俯仰 el_hat
-> 用 el_hat 做 32 层重聚焦
-> 再调用第 8.6 已成熟的 layer2 rank1 fallback
```

也就是：

> 不直接把层次三写成完整 3D/4D covfit，而是让二维信息先服务于公共俯仰估计，再回到已成熟的层次二 fallback。

### 7.2 power refocus

对候选俯仰 $e_0$ 做 32 层合成后，定义：

$$
P(e_0)=\frac{1}{QT}\sum_{q,t}\left|y_q^{(e_0)}(t)\right|^2
$$

取：

$$
\hat e_c=\arg\max_{e_0}P(e_0)
$$

然后用 $\hat e_c$ 重新合成：

$$
y_q^{(\hat e_c)}(t)=w_z^H(\hat e_c)x_{q,:}(t)
$$

最后调用第 8.6 的 layer2 `rank1 equal-phase covfit fallback`。

### 7.3 2D-MUSIC peak refocus

在同一 raw 65×32 数据上，2D-MUSIC 虽然未必能分出两个峰，但它的主峰俯仰位置仍可用于公共俯仰估计：

$$
\hat e_c=
el\left[
\arg\max_{\theta,e}P_{2D}(\theta,e)
\right]
$$

之后流程与 power refocus 相同，仍然回到 layer2 rank1 fallback。

### 7.4 rank1-objective refocus

第三种尝试是：

```text
扫描 e0
-> 每个 e0 下都跑一次 layer2 rank1 covfit
-> 选择 rank1 objective 最小的 e0
```

即：

$$
\hat e_c=\arg\min_{e_0}J_{\mathrm{rank1}}(e_0)
$$

事实证明，这条路不适合作为公共俯仰估计准则。

### 7.5 关键结果

| Route | joint success |
|---|---:|
| `level2 wrong el_assumed` | 0.000 |
| `old el_bank` | 0.193 |
| `2D MUSIC` | 0.000 |
| `old common-el covfit` | 0.473 |
| `common_el_refocus_power + rank1` | 1.000 |
| `common_el_refocus_2dmusic_peak + rank1` | 1.000 |
| `common_el_refocus_rank1_objective` | 0.187 |
| `oracle` | 1.000 |

对应的辅助指标：

| 指标 | 数值 |
|---|---:|
| `B_refocus_power_el_hat_abs_error_ge5` | 0.000 |
| `B_refocus_2dmusic_peak_el_hat_abs_error_ge5` | 0.000 |
| `B_refocus_rank1_objective_el_hat_abs_error_ge5` | 1.423 |
| `B_rank1_objective_valley_width_el_ge5` | 5.017 |

### 7.6 第 4 部分结论

这一部分直接给出第 8.7 的第一条明确工程主结论：

> 对于“同俯仰 + 强相干 + 小方位间隔 + `el_assumed` 错误”的场景，层次三最合理的用法不是直接做 3D/4D covfit，而是先从二维数据估计公共俯仰，再回到第 8.6 已成熟的 layer2 rank1 fallback。

具体结论是：

1. `power refocus` 和 `2D-MUSIC peak refocus` 都达到 oracle。
2. `rank1-objective refocus` 明显不适合作为 `el` 选择准则。
3. 层次三信息不一定要输出最终二维 DOA；它也可以只承担“纠正公共俯仰聚焦”的角色。

---

## 8. 第 8.7 第 5 部分：一般源模型下的分流边界验证

### 8.1 目标

第 5 部分的目标不是开发新算法，而是：

```text
在一般源模型下验证分流边界。
```

此时统一使用一般源模型

$$
s_2(t)=\beta e^{j\phi}\left(\rho s_1(t)+\sqrt{1-\rho^2}v(t)\right)
$$

比较的核心 route 是：

- `level2_music_or_center_real`
- `level2_rank1_fallback`
- `common_el_refocus_power_rank1`
- `level3_2d_music`
- `pair_el_local_covfit`
- `dispatch_recommended_route`

### 8.2 关键 keypoints

| 指标 | 数值 |
|---|---:|
| `rho_music_joint_rho1` | 0.000 |
| `rho_music_joint_rho0p7` | 0.333 |
| `beta_rank1_joint_beta1` | 1.000 |
| `beta_rank1_joint_beta0p3` | 0.453 |
| `beta_weak_boundary_rate_beta0p3` | 1.000 |
| `phase_rank1_joint_phase0` | 1.000 |
| `phase_rank1_joint_phase150` | 0.000 |
| `phase_anti_boundary_rate_phase150` | 1.000 |
| `large_el_2dmusic_joint_el5_clean` | 1.000 |
| `large_el_pair_covfit_joint_el5_clean` | 1.000 |
| `large_el_2dmusic_joint_el10_hard` | 1.000 |
| `dispatch_success_all` | 0.840 |
| `dispatch_low_confidence_all` | 0.182 |

### 8.3 第 5 部分总结出的四类边界

1. **非完全相干边界**  
   当 $\rho$ 下降时，MUSIC 有恢复趋势，但不是立刻完全恢复。  
   这说明“完全相干导致的秩退化”很重要，但不是唯一困难因素。

2. **弱目标边界**  
   当 `beta<=0.3` 时，rank1 route 成功率明显下降。  
   这类情形不应再被包装成“继续换个 covfit 模型就能解决”，而应进入弱目标 / SIC / 残差检测逻辑。

3. **近反相边界**  
   `phase=150/180 deg` 是明确病态边界。  
   这类问题的本质是 steering 组合相消，不适合继续用 equal-phase rank1 假设强行解释。

4. **大俯仰差边界**  
   `el_b>=5 deg` 的代表性 case 中，二维几何路线保持成功。  
   说明大俯仰差的处理应从层次二中剥离，交给 2D geometry branch。

### 8.4 第 5 部分的定位

第 5 部分的 `dispatch_recommended_route` 还使用了真值标签：

- `beta`
- `phase`
- `rho`
- `el_diff`

因此它的定位只能是：

> 一般源模型下的边界验证器。

它不是可直接部署的工程分流器，但它已经证明：

- MUSIC、rank1/refocus、2D geometry branch 各自适合的区域是不同的；
- 把所有情形硬塞进一个单一算法并不合理；
- “工程分流”是比“继续堆自由度”更自然的收束方向。

---

## 9. 第 8.7 第 6 部分：observable dispatch demo

### 9.1 目标

第 6 部分开始把第 5 部分的标签化分流，改造成只依赖观测量的分流 demo：

```text
把第 5 部分的标签化分流转换成只依赖观测量的分流 demo。
```

允许使用的输入不再包括 `beta/phase/rho/el_true/el_diff`，而只包括：

- 1D MUSIC 的峰数、峰距、峰显著性、谱宽；
- rank1 的 objective gap、residual、pair separation；
- refocus 的 power sharpness、lambda sharpness；
- 2D MUSIC 的二维峰结构；
- pair-local covfit 的 residual 和 confidence proxy；
- 特征值代理与 route 间冲突关系。

### 9.2 第 6 部分结果

| 指标 | 数值 |
|---|---:|
| `oracle_dispatch_success_all` | 0.798 |
| `observable_dispatch_success_all` | 0.728 |
| `observable_vs_oracle_gap_all` | -0.070 |
| `route_agreement_rate_all` | 0.590 |
| `low_confidence_rate_all` | 0.220 |
| `false_high_confidence_rate_all` | 0.068 |
| `boundary_missed_rate_all` | 0.057 |
| `rho_scan_observable_success` | 0.815 |
| `beta_scan_observable_success` | 0.397 |
| `phase_scan_observable_success` | 0.586 |
| `large_el_observable_success` | 1.000 |

### 9.3 第 6 部分结论

第 6 部分说明：

1. 观测量驱动 dispatch 已经接近 oracle-label dispatch；
2. `large_el` 分支工作稳定；
3. `beta scan` 与 `phase scan` 仍是最难的部分；
4. 最大问题不是成功率本身，而是：
   - `false-high-confidence` 还偏高；
   - `boundary_missed` 还没压下去；
   - `common_el_refocus` 的 route availability 还带有真值 gating 残留。

因此第 6 部分的定位是：

> 证明 observable dispatch 有潜力，但 high-confidence 误报和 boundary miss 仍需校准。

---

## 10. 第 8.7 第 6B 部分：去真值化 route gating 与置信度校准

### 10.1 目标

第 6B 部分是 observable dispatch 去真值化与安全校准的收束点。

它的目标不是提高“绝对成功率上限”，而是让 observable dispatch 更像可用于工程接口的保守分流器：

1. 去掉 `common_el_flag` 对 refocus route 计算的真值 gating；
2. refocus 对所有 scenario-SNR case 都计算；
3. observable dispatch 只根据观测量决定是否使用 refocus；
4. 收紧 high-confidence；
5. 引入 `boundary_unreliable`；
6. 把 `false-high-confidence` 和 `boundary-missed` 压到安全区间。

### 10.2 第 6B 的核心修正

#### 修正 1：去真值化 route availability

旧逻辑中：

```text
只有 true common-el case 才计算 refocus route
```

第 6B 改成：

```text
所有 scenario 都计算 common_el_refocus_power_rank1
-> observable dispatch 再根据观测量决定是否选用
```

这一步把“route 是否存在”与“仿真真值标签”彻底解耦。

#### 修正 2：收紧 high-confidence

第 6B 不再轻易给 `high`，而是：

- `pair_el_local_covfit` 只有在二维双峰、pair residual、confidence proxy 都足够好时才给 `high`；
- `common_el_refocus_power_rank1` 要同时满足更严格的 sharpness、residual、gap 条件；
- `level2_rank1_fallback` 基本默认只给 `medium`；
- 当 route 之间互相矛盾时，直接降为 `low`。

#### 修正 3：加入 `boundary_unreliable`

第 6B 不试图从观测量里“直接识别 beta=0.3 或 phase=150”，而是更保守地做：

```text
如果多个 route 互相冲突
或 rank1 objective 很平
或 2D / refocus / rank1 都没有给出一致支持
-> 输出 boundary_unreliable 或 low_confidence
```

### 10.3 最终对比表

| 指标 | 第 6 部分 | 第 6B 部分 | 变化 |
|---|---:|---:|---:|
| observable success | 0.728 | 0.732 | +0.004 |
| oracle gap | -0.070 | -0.066 | +0.004 |
| low confidence | 0.220 | 0.240 | +0.020 |
| false-high-confidence | 0.068 | 0.000 | -0.068 |
| boundary-missed | 0.057 | 0.000 | -0.057 |

### 10.4 第 6B 的关键结果

| 指标 | 数值 |
|---|---:|
| `oracle_dispatch_success_all` | 0.798 |
| `observable_dispatch_success_all` | 0.732 |
| `observable_vs_oracle_gap_all` | -0.066 |
| `route_agreement_rate_all` | 0.589 |
| `low_confidence_rate_all` | 0.240 |
| `boundary_unreliable_rate_all` | 0.090 |
| `false_high_confidence_rate_all` | 0.000 |
| `boundary_missed_rate_all` | 0.000 |
| `rho_scan_observable_success` | 0.815 |
| `beta_scan_observable_success` | 0.397 |
| `phase_scan_observable_success` | 0.603 |
| `large_el_observable_success` | 1.000 |
| `refocus_computed_all_scenarios_flag` | 1.000 |
| `num_cases_refocus_selected` | 14 |

### 10.5 第 6B 的最终判断

第 6B 满足当前收束标准：

1. `observable_dispatch_success = 0.732`
2. `oracle_dispatch_success = 0.798`
3. `gap = -0.066`
4. `false-high-confidence = 0`
5. `boundary-missed = 0`

因此可以给出当前第 8.7 的分流安全判断：

> 6B 版 observable dispatch 已经满足安全目标，可以作为当前第 8.7 的保守工程分流雏形。

这里“保守”的含义非常明确：

- 它优先避免高置信误报；
- 宁可多输出 `low_confidence` / `boundary_unreliable`，也不把弱目标和近反相伪装成正常成功 case；
- 它不是“所有一般源都能统一最优拟合”的算法，而是面向工程链路的稳健分流器。

---

## 11. 第 8.7 第 7 部分：预算感知级联早停策略验证

### 11.1 目标

第 7 部分不是新 DOA 算法，而是把第 6B 的全 route observable dispatch 改成预算感知级联早停策略。第 6B 为了验证规则，会离线计算 level2 MUSIC、level2 rank1、common-el refocus、2D MUSIC 和必要的 pair-local，再统一 dispatch；第 7 部分则验证工程执行时是否可以先跑低成本诊断，若 route 已可靠则早停，只在必要时触发重 route。

### 11.2 关键结果

| 指标 | 数值 |
|---|---:|
| `full_6b_success` | 0.732 |
| `cascade_dispatch_success` | 0.731 |
| `gap` | -0.001 |
| `false_high_confidence_rate` | 0.000 |
| `boundary_missed_rate` | 0.000 |
| `low_confidence_rate` | 0.228 |
| `mean_routes_evaluated` | 3.408 |
| `estimated_cost_reduction` | 0.355 |
| `music2d_trigger_rate` | 0.581 |
| `pair_local_trigger_rate` | 0.353 |
| `route_agreement_with_6b` | 0.988 |

### 11.3 定位

第 7 部分说明：在不改变第 6B 判据和 route 逻辑的前提下，级联早停可以几乎保持原始 success 与 safety，同时显著减少平均 route 数和 estimated cost。它的边界也很明确：第 7 部分仍然是 estimated cost / offline policy validation。脚本为了和 full dispatch 对比，仍会先计算多条 route，再模拟 cascade 会在哪里早停，因此它不是 MATLAB wall-clock runtime 验证。

---

## 12. 第 8.7 第 7B 部分：真实 lazy wall-clock runtime 验证

### 12.1 目标

第 7B 部分把第 7 部分的级联策略落实为真实 lazy execution：

- 如果前序 gate 已经选定 route，后续未触发 route 不调用；
- 正式计时前使用 1 个 warm-up trial，warm-up 不计入统计；
- per-trial runtime 只包含算法执行，不包含绘图、CSV/MAT 写盘；
- 每个 trial 记录 stage executed flag；
- 同时执行 full-route wall-clock baseline 与 lazy-cascade wall-clock runtime，用真实 MATLAB `tic/toc` 对比。

### 12.2 关键结果

| 指标 | full route | lazy cascade |
|---|---:|---:|
| mean runtime | 0.629 s | 0.405 s |
| median runtime | 0.630 s | 0.606 s |
| p90 runtime | 0.655 s | 0.650 s |
| p95 runtime | 0.666 s | 0.659 s |

| 指标 | 数值 |
|---|---:|
| `mean_runtime_reduction` | 0.359 |
| `median_runtime_speedup` | 1.027 |
| `lazy_success` | 0.731 |
| `full_success` | 0.732 |
| `success_gap` | -0.001 |
| `false_high_confidence_rate` | 0.000 |
| `boundary_missed_rate` | 0.000 |
| `low_confidence_rate` | 0.228 |

### 12.3 执行结构

| stage | execution rate |
|---|---:|
| level2 MUSIC | 1.000 |
| common-el refocus | 1.000 |
| level2 rank1 | 0.860 |
| 2D MUSIC | 0.581 |
| pair-local | 0.353 |
| early-stop | 0.772 |

按组统计显示：

| group | lazy success | mean lazy runtime |
|---|---:|---:|
| rho scan | 0.815 | 0.204 s |
| beta scan | 0.397 | 0.276 s |
| phase scan | 0.600 | 0.327 s |
| large-el | 1.000 | 0.644 s |

### 12.4 最终判断

第 7B 是当前第 8.7 工程 runtime 的最终主线版本。它在保持 success 和 safety 的同时，实现了真实平均运行时间下降。median、p90、p95 runtime 改善有限，主要原因是 `large-el` 场景仍必须执行 2D MUSIC 和 pair-local covfit；这说明当前尾部 latency 的瓶颈已经从分流逻辑转向重 route 本身。后续若做工程部署优化，应优先考虑 2D MUSIC / pair-local 的 ROI 限制、粗细网格、并行化、GPU、steering 预计算或网格降采样，而不是继续改变第 7B 的 dispatch 判据。

---

## 13. 最终工程分流框架

### 13.1 输入

输入是：

```text
65×32 圆柱阵观测数据
```

也就是保留二维圆柱阵观测，再决定走哪条 route。

### 13.2 候选 route

当前第 8.7 的工程候选 route 包括：

1. `level2 MUSIC / center-real`
2. `level2 rank1 fallback`
3. `common-el refocus + layer2 rank1`
4. `level3 2D MUSIC`
5. `pair-el local covfit`

对应功能分工如下：

| Route | 主要作用 |
|---|---|
| `level2_music_or_center_real` | 当层次二一维谱已可靠双峰时，保留 MUSIC 主链 |
| `level2_rank1_fallback` | 当 MUSIC 双峰塌缩，但仍处在强相干、近等幅、近同相、俯仰接近的层次二适用区间时，作为 fallback |
| `common_el_refocus_power_rank1` | 当公共俯仰可从二维信息重聚焦时，先估 `el_hat` 再回到层次二 rank1 |
| `level3_2d_music` | 当大俯仰差导致二维双峰明显时，直接走二维几何路线 |
| `pair_el_local_covfit` | 在 2D-MUSIC 基础上做局部 refinement，提高大俯仰差场景输出质量 |

### 13.3 观测指标

observable dispatch 当前使用的主要观测指标包括：

#### 1D MUSIC 指标

- `peak_count`
- `peak_separation`
- `peak_prominence_ratio`
- `spectrum_width`
- 特征值代理

#### rank1 指标

- `residual_norm`
- `score_gap_ratio`
- `pair_sep_est`

#### common-el refocus 指标

- `focused_power_peak_sharpness`
- `lambda1_peak_sharpness`
- `el_hat`

#### 2D MUSIC 指标

- `peak_count`
- `peak_separation_az`
- `peak_separation_el`
- `global_peak_el`

#### pair-local covfit 指标

- `residual_norm`
- `pair_local_confidence_proxy`
- `finite_output_flag`

#### 跨 route 一致性

- route 冲突关系；
- 是否存在明显 `boundary_unreliable` 迹象。

### 13.4 分流逻辑

当前最终分流框架可写成：

```text
if 2D-MUSIC 存在可靠二维双峰:
    使用 2D-MUSIC 或 pair-el local covfit

elif 1D MUSIC 双峰可靠:
    使用 level2 MUSIC

elif 二维信息显示公共俯仰可聚焦:
    使用 common-el refocus
    然后回到 layer2 rank1 fallback

elif rank1 fallback 输出可靠:
    使用 level2 rank1 fallback

else:
    输出 boundary_unreliable 或 low_confidence
```

### 13.5 工程 runtime 主线

第 7B 之后，最终工程链路不再建议按第 6B 的方式默认全 route 离线计算，而是采用真实 lazy cascade：

```text
Stage 1: level2 MUSIC 粗诊断
Stage 2: common-el refocus + layer2 rank1
Stage 3: level2 rank1 fallback
Stage 4: 2D MUSIC
Stage 5: pair-local refinement
Stage 6: low_confidence / boundary_unreliable
```

其中前序 stage 满足可靠判据时立即早停，后续 route 不调用。这个 runtime 架构保留第 6B 的 observable dispatch 安全目标，同时把第 7B 的 wall-clock mean runtime 从 `0.629 s` 降到 `0.405 s`。当前最终主线脚本是 `space_smooth_music_B_cylindrical_level3_lazy_runtime_wallclock.m`，短名 runner 是 `space_smooth_music_B_cylindrical_level3_lazy_runtime.m`。

### 13.6 最终框架的本质

这个框架有三个核心特征：

1. **不再依赖真值标签**  
   不再直接使用 `beta/phase/rho/el_true/el_diff` 作为工程输入。

2. **保留第 8.6 的层次二主链地位**  
   MUSIC 仍是默认主路线，rank1 仍是 fallback，而不是被层次三完全替代。

3. **把层次三信息用在最需要的地方**  
   - 大俯仰差：直接二维分辨；
   - 同俯仰但先验错误：先重聚焦，再回层次二。

因此，第 8.7 的最终算法链路不是“统一 4D 最优搜索”，而是：

> 层次三信息 + 层次二 fallback + 观测量 dispatch

---

## 14. 当前未解决问题和未来方向

### 14.1 当前未解决问题

第 8.7 已经阶段性收束，但以下边界仍未解决：

1. **弱目标**：尤其是 $\beta \le 0.3$；
2. **近反相**：尤其是 `phase=150/180 deg`；
3. **更复杂多目标场景**；
4. **colored noise / 更复杂噪声模型**；
5. **阵元误差与模型失配**；
6. **真实动态轨迹链路联动**；
7. **V2 constrained covariance fitting 尚未实现**；
8. **完整 4D az/el pair global covfit 未实现**。

### 14.2 未来方向优先级

后续工作若继续推进，建议优先级如下：

1. **弱目标 / SIC / 残差检测**
   - 这是当前最实在的失败边界；
   - 比继续堆 general covariance fitting 更贴近工程需求。

2. **近反相 derivative-steering 边界模型**
   - 用于解释和处理 steering 组合相消问题；
   - 比继续优化 equal-phase rank1 更有意义。

3. **更大 Monte Carlo 复核第 6B 指标**
   - 当前第 6B 已够收束；
   - 如果做论文最终版，可再扩大样本数复核。

4. **与真实 Step5 / Route-B 流水线联动**
   - 把 observable dispatch 放回上游输出链路中验证。

5. **最后才考虑 V2/general $R_s$**
   - 在当前阶段，这不是最优先方向；
   - 除非前四项都做完，否则不建议马上回去堆 V2。

---

## 15. 论文写法建议

### 15.1 不建议的写法

不建议写成：

```text
本文提出一个覆盖所有一般相干源的统一协方差拟合算法。
```

这会与当前第 8.7 的真实收束状态不符。

### 15.2 建议的写法

建议写成：

> 本文在 65×32 圆柱阵工程子阵下，构建了一个场景自适应的相干目标 DOA 处理链。该处理链保留 MUSIC 在可分峰场景下的优势，在完全相干小方位间隔下引入 rank1 fallback，在俯仰先验错误时利用二维信息进行 common-el refocus，在大俯仰差场景下转入 2D-MUSIC / pair-local covfit，并通过观测量驱动 dispatch 给出低置信度和边界场景保护。

### 15.3 需要强调的三点

论文中建议明确强调：

1. 当前不是万能算法；
2. 当前是保守工程分流雏形；
3. 弱目标和近反相仍是未来工作。

换句话说，第 8.7 的价值不在于“统一拟合所有一般相干模型”，而在于：

- 把第 8.6 层次二的边界讲清楚；
- 证明二维信息确实能补救这些边界；
- 最终给出可解释、可分流、可降置信度的工程链路。

---

## 16. 文件和代码索引

### 16.1 第 8.7 主线代码

| 第几部分 | 主线脚本 | 对应内容 |
|---|---|---|
| 第 1 部分 | `space_smooth_music_B_cylindrical_level3_minimal_validation.m` | 层次三最小验证 |
| 第 2 部分 | `space_smooth_music_B_cylindrical_level3_rank1_covfit_minimal.m` | 二维 rank1 covfit 非 oracle 最小验证 |
| 第 3 部分 | `space_smooth_music_B_cylindrical_level3_common_el_stability.m` | common-el 3D covfit 稳定性分析 |
| 第 4 部分 | `space_smooth_music_B_cylindrical_level3_common_el_refocus.m` | common-el refocus + layer2 fallback |
| 第 5 部分 | `space_smooth_music_B_cylindrical_level3_general_model_dispatch_boundary.m` | 一般源模型分流边界验证 |
| 第 6 部分 | `space_smooth_music_B_cylindrical_level3_observable_dispatch_demo.m` | 观测量驱动工程分流 demo |
| 第 6B 部分 | `space_smooth_music_B_cylindrical_level3_observable_dispatch_calibrated.m` | 去真值化 route gating 与置信度校准 |
| 第 7 部分 | `space_smooth_music_B_cylindrical_level3_cascade_dispatch_runtime.m` | 预算感知级联早停策略验证 |
| 第 7B 部分 | `space_smooth_music_B_cylindrical_level3_lazy_runtime_wallclock.m` | 真实 lazy wall-clock runtime 验证 |
| 第 7B 短名 runner | `space_smooth_music_B_cylindrical_level3_lazy_runtime.m` | 调用第 7B 主脚本 |

### 16.2 主线结果目录

| 第几部分 | 结果目录 |
|---|---|
| 第 1 部分 | `results_step8_7_level3_minimal_validation/` |
| 第 2 部分 | `results_step8_7_level3_rank1_covfit_minimal/` |
| 第 3 部分 | `results_step8_7_level3_common_el_stability/` |
| 第 4 部分 | `results_step8_7_4_common_el_refocus/` |
| 第 5 部分 | `results_step8_7_5_general_model_dispatch_boundary/` |
| 第 6 部分 | `results_step8_7_6_observable_dispatch_demo/` |
| 第 6B 部分 | `results_step8_7_6b_observable_dispatch_calibrated/` |
| 第 7 部分 | `results_step8_7_7_cascade_dispatch_runtime/` |
| 第 7B 部分 | `results_step8_7_7b_lazy_runtime_wallclock/` |

### 16.3 最终推荐阅读顺序

如果只想快速掌握第 8.7，建议按以下顺序阅读：

1. `第8.7步_层次三与观测量工程分流总结报告.md`
2. `第8.7步_7B_真实lazy级联运行时间验证记录.md`
3. `第8.7步_6B_观测量分流去真值化与置信度校准记录.md`
4. `第8.7步_4_common-el重聚焦与层次二fallback验证记录.md`
5. `第8.7步_5_一般源模型分流边界验证记录.md`

如果需要理解路线演化，再回读：

6. `第8.7步_1_层次三最小验证记录.md`
7. `第8.7步_2_二维rank1协方差拟合最小验证记录.md`
8. `第8.7步_3_common-el二维covfit稳定性验证记录.md`

### 16.4 本阶段已停止或暂不继续的方向

第 8.7 当前不建议继续马上推进：

- V2 constrained covariance fitting；
- complex-gain q 诊断或优化；
- 完整 4D az/el pair global covfit；
- fullscan 式大范围搜索。

这些方向不是永远无效，而是在当前阶段不再是最优投入点。

---

## 17. 暂不修改但需要说明的模型与算法边界

本节只列出当前阶段需要在论文和工程说明中明确交代的边界，不作为本轮代码修改项：

1. **observable dispatch 的经验阈值不是物理闭式判据**  
   `peak_prominence_ratio`、`spectrum_width`、`lambda2_over_noise`、`score_gap_ratio`、`residual_norm`、`focused_power_peak_sharpness`、`lambda1_peak_sharpness` 等阈值是基于仿真调出的可解释启发式规则。当前不改阈值。

2. **当前 SNR 是接收总 clean power SNR**  
   噪声按 $\sigma^2=\mathrm{mean}(|y_{clean}(:)|^2)/10^{SNR/10}$ 设置，不是单源固定功率 SNR。近反相能量抵消的专门评估需要另做固定噪声底或单源功率归一化实验。

3. **源信号生成不是严格 i.i.d. 复高斯源**  
   当前 `s1_base` 与 `v_base` 是确定性复指数基序列，适合边界趋势验证。若未来做更严格统计验证，可改为随机独立源或正交化源，但这会形成新实验阶段。

4. **2D MUSIC 使用中心子阵 steering**  
   当前 2D MUSIC 对二维平滑协方差使用 center-subarray steering 计算谱函数。这是合理 baseline，但不是严格的全子阵流形平均谱；当前不改。

5. **rank1 covfit 使用归一化 steering**  
   当前 covfit 适合 DOA 搜索，不做真实幅度估计。因此弱目标仍是边界，不在当前第 8.7 内求解。

6. **pair-local 的 az/el 排序稳健性需在未来窗口变化时复核**  
   当前参数下 az 局部扰动不会导致交叉，结果不受影响。若未来缩小方位间隔或扩大局部窗口，应检查 az 排序后 el 是否同步排序。

7. **pair-local 可作为未来 optional refinement 方向**  
   当前第 7B 主线保持 pair-local 的既有触发顺序。后续若做 runtime 优化，可评估将 pair-local 降级为 optional refinement，但这不在本轮修改。

8. **weak-target 和 near anti-phase 当前只做保护输出**  
   当前策略对弱目标和近反相输出 `low_confidence` / `boundary_unreliable`，不是求解。未来若要解决，需要新阶段的弱目标 SIC / 残差检测或 near anti-phase derivative-steering / 差分模态。

9. **V2/general covariance fitting 暂不推进**  
   V1 complex-gain 与 common-el 3D covfit 已显示可辨识性和 objective landscape 不稳。V2/general $R_s$ 保留为未来工作，不作为当前收束主线。

10. **runtime 优化应优先面向重 route 本身**  
    第 7B 显示 large-el 组是尾部耗时瓶颈。后续工程部署可考虑 ROI 限制、粗细网格、并行化、GPU、预计算 steering 或降低 2D MUSIC 网格，这些属于工程优化，不在本轮修改。

---

## 18. 最终结论

第 8.7 的最终结论可以压缩成以下六句话：

1. 第 8.7 不是单一的 2D-MUSIC 算法，而是围绕层次二边界展开的二维信息补救与工程分流研究。
2. 对于大俯仰差，二维几何分离路线 `2D-MUSIC / pair-el local covfit` 是正确方向。
3. 对于同俯仰但俯仰先验错误，最有效的路线不是直接 3D covfit，而是 `common-el refocus + layer2 rank1 fallback`。
4. 在一般源模型下，弱目标和近反相是明确边界，不能继续假装一个单一算法就能统一覆盖。
5. 第 6B 版 observable dispatch 已把第 8.7 收束为一个“层次三信息 + 层次二 fallback + 观测量驱动低置信度保护”的保守工程分流雏形。
6. 第 7B 版真实 lazy cascade 是当前工程 runtime 主线，它保持 success 与 safety，并在真实 wall-clock mean runtime 上取得下降。

这就是当前第 8.7 对整篇论文和工程链路的真正贡献。
