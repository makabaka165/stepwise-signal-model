继续在当前项目的中推进第 8.6 步层次二第六部分。

当前已有提交：

- 第四部分：0a36280，一维圆柱协方差拟合验证；
- 第五部分：5d7e3e8，rank1 equal-phase covfit 稳定性与偏差扫描。

第五部分结论：

1. rank1 equal-phase covfit 已成为层次二当前主算法候选；
2. 在 sep=10, SNR=18:2:30, K_phi=[16,20,24,28] 下，rank1 tol_abs01 全部达到 1.000；
3. rank1 sep=10 的 SNR90_abs01 在当前测试网格下为 18 dB，但由于最低测试点就是 18 dB，因此真实门槛只能写成 SNR90 <= 18 dB；
4. K_phi=28 在 SNR90 并列最低时 separation error 最小；
5. K=28, sep=10, SNR=30 下 mean_abs_pair_sep_error≈0.031 deg，objective valley 不算过宽；
6. phase offset 0/5/15/30/60 deg 下 rank1 均保持 tol_abs01=1.000；
7. 下一步需要做最终验证扫描，确定真实 SNR90 和 rank1 covfit 的整体性能曲线。

本轮目标：

只做“层次二第六部分：rank1 covfit final validation scan”。

不要做层次三。
不要做 2D az/el MUSIC。
不要做 Hermitian LS 优化。
不要做 phase-scan covfit。
不要做 PME/SBL/SPICE/DML。
不要修改第 7.5、第 8、第 8.5 步旧代码。
不要覆盖前五部分结果。

---

## 一、新增脚本和结果目录

新增脚本：

stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/space_smooth_music_B_cylindrical_level2_rank1_covfit_finalscan.m

新增结果目录：

stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/results_step8_6_level2_rank1_covfit_finalscan/

新增记录文档：

stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/第8.6步_层次二第六部分_rank1协方差拟合最终验证记录.md

必须读取并参考：

- 第五部分脚本：
  stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/space_smooth_music_B_cylindrical_level2_rank1_covfit_scan.m

- 第五部分结果：
  stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/results_step8_6_level2_rank1_covfit_scan/step8_6_level2_rank1_covfit_scan_summary.csv
  stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/results_step8_6_level2_rank1_covfit_scan/step8_6_level2_rank1_covfit_scan_keypoints.csv
  stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/第8.6步_层次二第五部分_rank1协方差拟合稳定性扫描记录.md

---

## 二、本轮 route

只保留三个 route：

1. center_real
2. min_den_fb
3. covfit_rank1_equal_phase

不要再跑 Hermitian LS。

---

## 三、第一阶段：低 SNR 精细扫描，确定真实 SNR90

固定：

```matlab
K_phi = 28;
sep_factor_list = [7, 10];
snr_list = 6:2:20;
Metkl = 200;
T_snap = 260;
```

目的：

确定：

```
rank1 covfit 在 sep=10 的真实 SNR90
```

注意：

如果在最低点 6 dB 仍然 >=0.9，则继续追加低 SNR：

```
snr_list_extra = -4:2:6;
```

可以分段执行，但必须记录。

输出：

```
snr90_abs01
snr90_rel025
```

如果最低测试点已经达到 90%，必须写成：

```
SNR90 <= lowest_tested_snr
```

不要写成恰好等于最低点。

------

## 四、第二阶段：sep_factor 完整曲线

固定：

```
K_phi = 28;
sep_factor_list = [1, 2, 3, 5, 7, 10];
snr_list = -4:2:20;
Metkl = 200;
T_snap = 260;
```

只跑：

```
center_real
min_den_fb
covfit_rank1_equal_phase
```

如果计算量过大，优先保证 covfit_rank1，并对 center_real/min_den_fb 只保留关键 SNR 点：

```
snr_list_baseline = [10, 14, 18, 20]
```

但如果可行，三条 route 都跑完整。

目标：

生成第 8.6 层次二最终曲线：

```
sep_factor vs SNR90
```

并和旧 MUSIC 基线对比。

------

## 五、必须统计的指标

每个 route、K、sep、SNR 必须统计：

```
raw_success_rate
tol_success_rate_abs01
tol_success_rate_rel025
rmse_deg
degraded_rate
snr90_abs01
snr90_rel025
mean_pair_sep_est
mean_pair_sep_bias
mean_abs_pair_sep_error
mean_pair_center_bias
mean_abs_pair_center_error
objective_true_rank_median
objective_true_rank_p90
objective_valley_sep_width_mean
objective_valley_center_width_mean
```

对 covfit_rank1，必须记录：

```
mean_objective_best
mean_objective_true_pair
objective_margin_true_vs_best
```

------

## 六、结果文件

结果目录：

stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/results_step8_6_level2_rank1_covfit_finalscan/

必须生成：

1. step8_6_level2_rank1_covfit_finalscan.log
2. step8_6_level2_rank1_covfit_finalscan_summary.csv
3. step8_6_level2_rank1_covfit_finalscan_keypoints.csv

keypoints 至少包含：

```
K=28
sep=1/2/3/5/7/10
SNR around SNR90
三条 route
```

1. step8_6_level2_rank1_covfit_finalscan_result.mat
2. 至少 5 张图：

```
rank1_final_snr90_vs_sep.png
rank1_final_tol_vs_snr_sep10.png
rank1_final_rmse_vs_snr_sep10.png
rank1_final_sep_bias_vs_snr.png
rank1_final_compare_music_baselines.png
```

1. 新增记录文档：

stepwise_signal_model/steps/step_08_6_routeB_array_level2_3/第8.6步_层次二第六部分_rank1协方差拟合最终验证记录.md

------

## 七、判断标准

### 情况 A：rank1 在 sep=10 的真实 SNR90 明显低于 18 dB

如果发现：

```
rank1 sep=10 SNR90_abs01 <= 12 dB
```

则写：

```
rank1 covfit 显著优于现有所有层次二 MUSIC 方案，是第 8.6 步主算法候选。
```

### 情况 B：rank1 在 sep=10 的真实 SNR90 约 14~18 dB

仍然是好结果，但写：

```
rank1 covfit 明显优于 MUSIC 谱函数族，但极小间隔仍有一定 SNR 门槛。
```

### 情况 C：低 SNR 下 separation bias 明显扩大

如果：

```
mean_abs_pair_sep_error > 0.08 deg
objective_valley_sep_width_mean 很大
```

则写：

```
rank1 covfit 在低 SNR 下存在 separation valley 宽化，需要后续研究 pair separation regularization。
```

### 情况 D：rank1 在 sep=1/2/3/5/7/10 全部稳定

则写：

```
层次二 rank1 covfit 已经可以作为完整 65 列工程子阵的一维方位超分辨主路线。
```

------

## 八、本轮不要做的事情

1. 不做层次三；
2. 不做 2D az/el；
3. 不做 Hermitian LS；
4. 不做 phase-scan covfit；
5. 不做真实 Step 5 联动；
6. 不修改旧第 7.5、第 8、第 8.5 步；
7. 不覆盖前五部分结果。

------

## 九、最终回答必须包含

完成后回答：

1. 新增文件列表；
2. 是否修改旧文件；
3. 低 SNR 精细扫描结果；
4. sep=10 的真实 SNR90；
5. sep_factor 完整曲线；
6. rank1 与 center_real/min_den_fb 的 SNR90 对比；
7. separation bias 是否在低 SNR 下扩大；
8. rank1 是否可以作为第 8.6 层次二最终主算法；
9. 是否还需要层次三。

再次强调：

本轮只做第 8.6 步层次二 rank1 covfit 最终验证。
 不要做层次三。
 不要做 2D az/el。
 不要做 Hermitian LS。
 不要做 phase-scan covfit。
